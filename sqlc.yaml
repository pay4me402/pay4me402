version: "2"
sql:
  - engine: "sqlite"
    schema: "internal/db/migrations"
    queries: "sql/queries"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_json_tags: true
